<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="create-table-expense" author="AD Coder">
        <comment>Create table expense</comment>
        <createTable tableName="expense">
            <column name="id" type="BIGINT" autoIncrement="true" generationType="ALWAYS">
                <constraints primaryKey="true" primaryKeyName="pk_expense" nullable="false"/>
            </column>

            <column name="user_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_expense_user_id" references="auth_user(id)"/>
            </column>

            <column name="title" type="VARCHAR(128)">
                <constraints nullable="false" />
            </column>

            <column name="category" type="VARCHAR(64)">
                <constraints nullable="false" />
            </column>

            <column name="amount" type="BIGINT">
                <constraints nullable="false" />
            </column>

            <column name="date" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="${NOW}">
                <constraints nullable="false" />
            </column>

            <column name="description" type="TEXT" />

            <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="${NOW}">
                <constraints nullable="false" />
            </column>

            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="${NOW}">
                <constraints nullable="false" />
            </column>
        </createTable>
    </changeSet>

</databaseChangeLog>